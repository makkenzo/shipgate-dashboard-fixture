name: Shipgate CI

on:
  push:
    branches:
      - main
      - develop
      - stage5/v1/source

  pull_request:
    branches:
      - develop
      - stage5/v1/source

jobs:
  verify:
    name: shipgate/verify
    runs-on: ubuntu-latest

    steps:
      - name: Verify fixture
        run: echo "Shipgate fixture is healthy"
