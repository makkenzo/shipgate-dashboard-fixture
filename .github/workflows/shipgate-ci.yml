name: Shipgate CI

on:
  push:
    branches:
      - main
      - develop

  pull_request:
    branches:
      - develop

jobs:
  verify:
    name: shipgate/verify
    runs-on: ubuntu-latest

    steps:
      - name: Verify fixture
        run: echo "Shipgate fixture is healthy"
